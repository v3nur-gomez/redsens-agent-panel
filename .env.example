# Copy this file to .env and set the values for production/local testing
# Full API Gateway URL (optional - defaults to the URL you provided)
VITE_BEDROCK_API_URL=https://7dtlerya6j.execute-api.us-east-1.amazonaws.com
# Optional path appended to the base URL (for example 'prod/rlm' or 'prod/bedrock')
VITE_BEDROCK_API_PATH=
# If your API requires an API key, set it here (will be sent in the header defined below)
VITE_BEDROCK_API_KEY=
# Header name to send the API key in (defaults to x-api-key)
VITE_BEDROCK_API_KEY_HEADER=x-api-key
